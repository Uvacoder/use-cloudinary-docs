---
title: useAudio
---
The `useAudio` hook gives you the ability to call an MP3 from Cloudinary and apply transformations. The result is a URL generated by the SDK. 

Cloudinary treats audio files the same as video files, meaning you can also call the audio from a video inside your account.

```jsx
import { useAudio } from 'use-cloudinary';

function Audio({ cloudName, publicId, transforms }) {
  const { getAudio, data, status, error } = useImage({ cloud_name: cloudName });
  
  React.useEffect(() => {
    getAudio({
      public_id: publicId, 
      transform_options: {
        ...transforms
      }
    })
  }, [])

  if (status === "loading") return <p>Loading...</p>;
  if (status === "error") return <p>{error.message}</p>;

  return (
    <audio>
      <source src={data} />
    </audio>
  )
}
```

## Args

<div style="overflow-x:auto;">
  <table>
    <tr>
      <th>Key</th>
      <th>Type</th>
      <th>Required?</th>
      <th>Description</th>
    </tr>
    <tr>
      <td>cloud_name</td>
      <td>String</td>
      <td>Yes</td>
      <td>Your Cloudinary cloud name, used to identify where we're getting our media from</td>
    </tr>
  </table>
</div>

## Return values

<div style="overflow-x:auto;">
  <table>
    <tr>
      <th>Key</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
    <tr>
      <td>getAudio</td>      
      <td>Func</td>
      <td>An async function that accepts options and returns the URL of your Cloudinary asset</td>
    </tr>
    <tr>
      <td>data</td>
      <td>undefined || String</td>      
      <td>When getAudio is successful, data will return the value of the request, usually a String defining your asset URL. More about React Query's return values <a href="https://github.com/tannerlinsley/react-query#returns">here</a></td>
    </tr>
    <tr>
      <td>status</td>
      <td>String</td>      
      <td>The status value defines the lifecycle of your request, returning either "loading", "error", or "success". More about React Query's return values <a href="https://github.com/tannerlinsley/react-query#returns">here</a></td>
    </tr>
    <tr>
      <td>error</td>
      <td>Object</td>      
      <td>When a server error is returned, the error is surfaced to the error object. More about React Query's return values <a href="https://github.com/tannerlinsley/react-query#returns">here</a></td>
    </tr>
  </table>
</div>

## getAudio()

The `getAudio` function is just a `setState`, used to fire off our internal query and pass our options into our SDK to generate our URL.

<div style="overflow-x:auto;">
  <table>
    <tr>
      <th>Key</th>
      <th>Type</th>
      <th>Required</th>
      <th>Description</th>
    </tr>
    <tr>
      <td>public_id</td>      
      <td>String</td>
      <td>Yes</td>
      <td>The public id is the name used as a title for the media you want to call</td>
    </tr>
    <tr>
      <td>transform_options</td>
      <td>Object</td>  
      <td>No</td>    
      <td>Video transformations apply for audio as well (e.g., trimming), but dimensional or visual related transformations are ignored. You can even <a href="https://cloudinary.com/documentation/audio_transformations#customizing_the_waveform_image">generate waveforms</a> from your audio</td>
    </tr>
  </table>
</div>

